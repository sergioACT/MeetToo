specVersion: "v1beta"
serviceId: "www"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "www-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
